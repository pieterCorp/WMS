<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <disabledPackageSources>
    <!-- Prevent restore failures when the corporate feed isn't reachable (e.g., off VPN). -->
    <add key="DMVH Package source" value="true" />
  </disabledPackageSources>
</configuration>
